#! /bin/bash

# Copy config files to ~/.config
cp -r ./BetterDiscord ~/.config
cp -r ./cava ~/.config
cp -r ./hypr ~/.config
cp -r ./kitty ~/.config
cp -r ./omposh ~/.config
cp -r ./rofi ~/.config
cp -r ./spicetify ~/.config
cp -r ./waybar ~/.config
cp -r ./wlogout ~/.config
